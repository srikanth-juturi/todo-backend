name: quality-gates

on:
  pull_request:
  push:
    branches: [main, develop]

jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies (stub)
        run: |
          python -m pip install --upgrade pip
          echo "TODO: install project dependencies"

      - name: Format check (stub)
        run: echo "TODO: black --check ."

      - name: Lint (stub)
        run: echo "TODO: ruff check ."

      - name: Type check (stub)
        run: echo "TODO: mypy app"

      - name: Test (stub)
        run: echo "TODO: pytest -q"
